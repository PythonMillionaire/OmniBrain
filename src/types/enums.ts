export enum MessageSender {
    Agent = "Agent",
    User = "User"
}

export enum MessageExtremity {
    top = "top",
    bottom = "bottom"
}

export enum ButtonPosition {
    top = 'top',
    bottom = 'bottom',
    right = 'right',
    left = 'left'
}
